name: GitHub Actions Demo

on:
  pull_request:
    branches:
      - "**"

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Showing message
        run: echo "🎉 A PR has created."
